-- Noirbound: Animation Component

---@module "libs.external.concord.concord"
local concord = require "libs.external.concord.concord"

local component = concord.component("animation", function (self)
    -- Todo: Finish this component.
end) 

return component
